public class HelloNumbers {
    public static void main(String args[]) {
        int x;//1,2
        x=0;
        while(x<10){
            System.out.println(x);
            x=x+1;
        }
    }
}
/**
1. Before Java variables can be used, they must be declared.
2. Java variables must have a specific type, like int.
3. Java variable types can never change. Once it is a int, it's always an int.
4. Types are varified before the codes even runs! If it fails to match, then all the codes won't run.
*/
